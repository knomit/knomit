// Placeholder for the Settings form. Kept separate from settings.tsx (which
// only mounts it) so it can be rendered in a test without a real #root element.
export function SettingsApp() {
  return <h1>Settings</h1>
}
