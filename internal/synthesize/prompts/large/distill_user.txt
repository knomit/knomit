You are synthesizing facts in a knowledge base to find patterns and higher-order insights.

Facts in scope:
{{.Facts}}

{{if .ApplicableMethodology}}Applicable methodology (reasoning lessons from prior synthesis cycles):

{{.ApplicableMethodology}}

Apply these lessons to the distillation. If a lesson informed how you weighted, combined, or named the synthesis, cite it in the output's refs.

{{end}}Identify patterns across these facts. Produce:
1. New higher-order facts that capture patterns
2. Which original facts are fully subsumed and can be forgotten

Respond as JSON (no markdown wrapping):
{
  "synthesize": [
    {
      "path": "{{.OntologyRoot}}/...",
      "title": "...",
      "body": "...",
      "type": "synthesis|methodology|observation|concept|process|principle|pattern|reference",
      "domain": [],
      "confidence": 0.X,
      "entities": [],
      "refs": ["source-file1.md", "source-file2.md"]
    }
  ],
  "retract": ["file1.md", "file2.md"]
}

Path and type are MANDATORY:
- Every output fact's path MUST start with `{{.OntologyRoot}}/`. Outputs with any other prefix are rejected.
- Every output fact MUST set `type` to one of the listed values. Outputs with empty or missing type are rejected. Default to "synthesis" for higher-order facts derived from observations.

## Hypothesis handling

- Do NOT use hypothesis-type facts as inputs for creating new synthesis facts. Hypotheses are unconfirmed predictions.
- If a hypothesis is confirmed by other evidence in the batch, retract it and include the hypothesis path in the new synthesis fact's refs.
- When all input facts are type "methodology", the output must also be type "methodology" — not "synthesis". Methodology facts are about reasoning process, not domain content. Always use a path under `{{.OntologyRoot}}/meta/reasoning/` for methodology-type output (e.g. `{{.OntologyRoot}}/meta/reasoning/descriptive-name.md`).